steps:
  - uses: actions/checkout@v1
  - uses: ricardochaves/python-lint@v1.4.0
    with:
      python-root-list: "."
      use-flake8: true
      use-pylint: false
      use-pycodestyle: false
      use-black: false
      use-mypy: false
      use-isort: false